[project]
name = "img-detection"
version = "0.1.0"
dependencies = [
    "discord.py>=2.3.0",
    "aiohttp>=3.8.0",
    "Pillow>=9.0.0",
    "aiosqlite>=0.19.0",
    "python-dotenv>=1.0.0",
    "numpy>=1.24.0",
]

[tool.hatch.build.targets.wheel]
packages = []